# the last statement in is returned implicitly
fn get_const() { 15 }

let my_const = call get_const()

fn fib(x) {
  if x <= 1 {
    ret x
  } else {
    ret call fib(x-1) + call fib(x-2)
  }
}

let my_fib = call fib(my_const)

my_fib




